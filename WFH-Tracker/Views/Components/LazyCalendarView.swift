import SwiftUI

// Use the existing CalendarView for now since it already works
// This provides the lazy loading benefits through LazyVGrid usage in the main view
typealias LazyCalendarView = CalendarView